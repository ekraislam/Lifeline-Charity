version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: lifeline_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_DATABASE: lifeline_db
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      # Initialization scripts mapped to initdb directory
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./backend/init-admin.sql:/docker-entrypoint-initdb.d/02-init-admin.sql
    networks:
      - lifeline-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lifeline_backend
    restart: always
    env_file:
      - ./backend/.env
    environment:
      - DB_HOST=db
      # Override DB variables to match the container setup
      - DB_USER=root
      - DB_PASSWORD=${DB_PASSWORD:-secret}
      - DB_NAME=lifeline_db
    volumes:
      - backend_uploads:/app/uploads
    ports:
      - "5000:5000"
    networks:
      - lifeline-network
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lifeline_frontend
    restart: always
    ports:
      - "80:80"
    networks:
      - lifeline-network
    depends_on:
      - backend

networks:
  lifeline-network:
    driver: bridge

volumes:
  db_data:
  backend_uploads:
